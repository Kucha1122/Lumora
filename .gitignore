bin/
obj/
.vs/
*.user
.blobs/

# Secrets — connection strings, peppers, API keys. Local dev config belongs in
# `dotnet user-secrets` (see src/Api/README-secrets.md), not in tracked files.
# Production config comes from Kubernetes Secrets, never from the repo.
appsettings.*.json
!appsettings.json
!appsettings.Development.json
appsettings.Local.json
appsettings.*.local.json
secrets.json
*.secrets.json
.env
.env.*
*.pfx
*.p12
*.key
*.pem
k8s/secrets.yaml

# Reference material copied in from another project — local only, not part of this repo.
.claude/rules/deploy-instructions-template.md
